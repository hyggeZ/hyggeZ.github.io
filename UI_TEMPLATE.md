# 技术笔记网站 UI 模板

## 用途

这套模板适合把 Word 或 Markdown 技术笔记转换为分章节静态网站，并发布到 GitHub Pages。

## 页面结构

- `index.html`：笔记首页和章节卡片目录。
- `chapters/chapter-XX.html`：每个大章节一个独立页面。
- `styles.css`：颜色、字号、间距、代码块和响应式布局。
- `script.js`：深色模式、阅读进度、代码复制和 Go 语法着色。
- `assets/`：只存放无法可靠转换成文字的结构图或示意图。

## 内容层级

- `一、二、三`：独立页面及页面主标题。
- `1. 2. 3.`：轻度强调的小节标题。
- `（1）（2）（3）`：与正文接近的说明层级。
- 可执行命令使用 `language-shell` 代码块。
- Go 代码使用 `language-go` 代码块，并保留复制按钮。

## 视觉规范

- 使用低饱和绿色作为强调色。
- 正文保持宽松字距，但小节间距紧凑。
- 浅色模式使用浅灰绿色代码背景；深色模式使用近黑绿色背景。
- 桌面端使用固定章节侧栏；窄屏隐藏侧栏并采用单栏正文。
- 代码块顶部栏保持紧凑，不使用装饰性图标。

## 复用方法

复制整个目录后，替换首页标题、章节卡片和 `chapters` 内的内容。保留 `styles.css` 与 `script.js`，即可延续相同 UI 和交互。
